Start the run.

Read `LOGBOOK.md` and `results.jsonl` and summarise where things stand. If they are
empty, this is the first run.

Measure `setting=5.0` first, with one replicate. That gives you a reference response and
shows you the shape of the returned metrics.

Then open your first cycle.

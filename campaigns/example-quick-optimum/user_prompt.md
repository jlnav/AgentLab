Start the run.

Read `LOGBOOK.md` and `results.jsonl` and summarise where things stand. If they are
empty, this is the first run.

Then open your first cycle. Submit the jobs it needs together, not one at a time, and
keep at least half the budget back — the first sweep says where to look, and following
that up is worth more than a finer grid you cannot afford to act on.

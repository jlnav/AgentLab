Start the run.

Read `LOGBOOK.md` and `results.jsonl` and summarise where things stand. If they are
empty, this is the first run.

Measure the baseline first: `level=6, strategy=default, memlevel=8, windowlog=15`. That
gives you a reference `total_seconds` and shows you the shape of the returned metrics.

Then open your first cycle.
